# Frontend environment variables
# Copy to .env.local (or set in hosting provider) and adjust as needed.

# URL of the Express backend (no trailing slash). Examples:
#   Local dev:  http://localhost:4000
#   Production: https://api.flexmore.com
VITE_API_URL=http://localhost:4000
